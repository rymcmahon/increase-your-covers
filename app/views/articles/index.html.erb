<% content_for :head do %>
  <title>CoverCentric Blog</title>
  <meta name="description" content="CoverCentric’s blog about how restaurants can use email marketing and website design to acquire new customers and keep them coming back.">
<% end %>
<div class="container">
  <div class="row">
    <div class="col s12 m10 offset-m1">
      <h2 class="center">Coming soon...</h2>
      <% @articles.each do |article| %>
        <%= sanitize article.title %>
        <%= sanitize article.body.truncate(200, separator: ' ') %>
      <% end %>
    </div>
  </div>
</div>
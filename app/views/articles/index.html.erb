<div class="container">
  <div class="row">
    <div class="col s12 m10 offset-m1">
      <% @articles.each do |article| %>
        <%= article.title.html_safe %>
        <%= article.body.truncate(200, separator: ' ').html_safe %>
      <% end %>
    </div>
  </div>
</div>
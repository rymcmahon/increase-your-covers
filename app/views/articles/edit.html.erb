<div class="container">
  <div class="row">
    <%= form_for :article, url: article_path(@article), method: :patch, :html => {:class => "col s12"} do |f| %>
      <% if @article.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>
          <ul>
            <% @article.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <div class="row">
        <div class="input-field col s10">
          <%= f.label :title %>
          <%= f.text_field :title %>
        </div>
      </div>
      <div class="row">
        <div class="input-field col s10">
          <%= f.label :body %>
          <%= f.text_area :body, class:"materialize-textarea", id:"textarea" %>
        </div>
      </div>
      <div class="row">
        <div class="col s4">
          <%= f.submit "Create Article", class:"btn waves-effect waves-light" %>
        </div>
      </div>
    <% end %>
    <div class="row  destroy-article-btn">
      <div class="col s4">
        <%= link_to 'Destroy', article_path, method: :delete, class:"btn destroy-btn", data: { confirm: 'Are you sure?' } %>
      </div>
    </div>
  </div>
</div>






